image: docker:latest

services:
  - docker:dind

stages:
  # - build
  - deploy

build:
  stage: build
  script:
    - docker build -t python_ucb .
  tags:
    - docker

deploy:
  stage: deploy
  script:
    - docker run --rm -v ~/.ssh/id_rsa:/root/.ssh/id_rsa mdsol/docker-ssh-exec pwd
  # only:
  #   - production
  tags:
    - docker
