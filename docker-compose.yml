version: '2'
services:
  web:
    build: .
    ports:
      - '443:443'
    volumes:
      - ./storage:/storage
      - /etc/letsencrypt/live/python-ucb/server.crt:/cert.pem
      - /etc/letsencrypt/live/python-ucb/server.key:/cert.key
